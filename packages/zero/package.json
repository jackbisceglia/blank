{
  "name": "@blank/zero",
  "version": "0.0.0",
  "type": "module",
  "exports": {
    ".": "./schema.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "clean:sqlite": "rm sync-replica.db sync-replica.db-shm sync-replica.db-wal sync-replica.db-wal2",
    "dev": "zero-deploy-permissions && zero-cache",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix",
    "format": "prettier --write ."
  }
}
